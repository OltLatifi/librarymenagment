@using Microsoft.AspNetCore.Identity
@model IdentityUser
@using librarymenagment.Helpers

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>User</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>
    <form class="row" method="post" asp-action="DetailsConfirmed" asp-controller="User" asp-area="Admin">
        <input type="hidden" name="id" value="@Model.Id" />
        <div class="col-12 col-md-6 form-group">
            <label class="control-label">Book</label>
            <select name="bookId" class="form-control" asp-items="@(ViewData["BookId"] as SelectList)">
                <option value="">-- Select a Book --</option>
            </select>
            <button type="submit" class="btn btn-primary">Add Permission</button>
        </div>
    </form>
</div>
<div>
    <a asp-area="Admin" asp-action="Index">Back to List</a>
</div>
